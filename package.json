{
  "name": "trackly-sand-ops",
  "version": "1.0.0",
  "private": true,
  "description": "Role-based sand mining vehicle and loading activity tracker backed by Google Sheets and Drive.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "check": "node --check server.js && node --check src/app/appServer.js && node --check src/core/env.js && node --check src/core/logging.js"
  },
  "dependencies": {
    "express": "^4.19.2",
    "formidable": "^3.0.0",
    "google-auth-library": "^10.9.0",
    "googleapis": "^144.0.0"
  },
  "engines": {
    "node": ">=18"
  }
}
